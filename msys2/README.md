Execute `msys2-sshd-setup.sh` in MSYS2 bash to register msys2 sshd.

Copy `MSYS2.lnk` to `$HOME\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Scoop Apps` to override MSYS2 by using Windows Terminal.
